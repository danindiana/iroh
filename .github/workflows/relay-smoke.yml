name: Relay Smoke Test

on:
  push:
    branches: [ main ]
    paths:
      - 'iroh-relay/**'
      - 'scripts/relay-smoke-test.sh'
      - 'scripts/local-relay-setup.sh'
      - '.github/workflows/relay-smoke.yml'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: relay-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: ${{ matrix.os }} / ${{ matrix.rust }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.rust == 'nightly' || (matrix.os != 'ubuntu-22.04' && matrix.os != 'ubuntu-latest') }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-latest, macos-latest, windows-latest]
        rust: [stable, beta, nightly]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Rust (${{ matrix.rust }})
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: clippy
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      - name: Run relay smoke test (Linux)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          bash scripts/relay-smoke-test.sh
      - name: Build relay (non-Linux)
        if: ${{ !startsWith(matrix.os, 'ubuntu') }}
        shell: bash
        run: |
          rustc -V
          cargo build -p iroh-relay
      - name: Upload relay logs on failure (Linux)
        if: failure() && startsWith(matrix.os, 'ubuntu')
        uses: actions/upload-artifact@v4
        with:
          name: relay-logs-${{ matrix.os }}-${{ matrix.rust }}
          path: |
            tmp/relay-smoke.log
          if-no-files-found: ignore
